# Sample Passport Okta OAuth App.

## Getting Started 

1. Run `npm install` to install packages
2. Copy `config/config.json` and rename to `config/config.json`. Use this config file to set Okta related environment variables.
3. `npm start` will start up the server at `localhost:3000`
